{
  "name": "svg.rx.js",
  "version": "0.0.7-alpha.1",
  "description": "Making SVG elements draggable, using RxJS.",
  "keywords": [
    "svg",
    "vector",
    "dragging",
    "touch"
  ],
  "main": "src/main.js",
  "files": [
    "/src"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/akauppi/svg.rx.js"
  },
  "bugs": {
    "url": "https://github.com/akauppi/svg.rx.js/issues"
  },
  "license": "MIT",
  "//scripts": {
    "//test:headless": "npx cypress run",
    "//test:headless2": "start-server-and-test 'sirv test --port 3092' 3092 'cypress run'",
    "//test:open": "npx cypress open",
    "//test:open2": "start-server-and-test \"sirv test --port 3092\" 3092 \"cypress open\""
  },
  "scripts": {
    "build": "npm run demo:build",
    "dev": "npm run demo:dev",
    "test": "npm run cy:run",
    "demo:build": "rollup -c",
    "demo:dev": "rollup -c -w",
    "demo:_start": "sirv public --single --host",
    "~~~gh-pages": "gh-pages -d public",
    "pregh-pages": "npm run demo:build",
    "test:_build": "rollup --config rollup.test.config.js",
    "cy:run": "start-server-and-test 'sirv test --port 3092 --single' 3092 'npx cypress run'",
    "cy:open": "start-server-and-test 'sirv test --port 3092 --single' 3092 'npx cypress open'",
    "//test:open": "npm run test:_build & concurrently --names sirv,cypress \"sirv test --port 3092 --single\" \"npx cypress open\"",
    "test:dev": "concurrently --restart-tries 10 --names rollup,sirv \"rollup --config rollup.test.config.js -w\" \"sirv test --port 3000 --single --dev\"",
    "precy:run": "npm run test:_build",
    "---precy:open": "npm run test:_build",
    "prepublishOnly": "npm test",
    "demo:clean": "(rm public/bundle/* public/bundle.css 2> /dev/null || true)",
    "test:clean": "(rm test/bundle/* test/bundle.css 2> /dev/null || true)",
    "clean": "concurrently \"npm run demo:clean\" \"npm run test:clean\""
  },
  "dependencies": {
    "rxjs": "^6.5.4",
    "sirv-cli": "^0.4.4",
    "svelte-filerouter": "^1.4.6"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "concurrently": "^5.0.2",
    "cypress": "^3.8.1",
    "gh-pages": "^2.1.1",
    "rollup": "^1.27.14",
    "rollup-plugin-commonjs": "^10.1.0",
    "rollup-plugin-livereload": "^1.0.0",
    "rollup-plugin-node-builtins": "^2.1.2",
    "rollup-plugin-node-globals": "^1.4.0",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-svelte": "^5.1.1",
    "rollup-plugin-terser": "^5.1.2",
    "rollup-pluginutils": "^2.8.2",
    "start-server-and-test": "^1.7.13",
    "svelte": "^3.16.7"
  },
  "//comments": {
    "": [
      "Based on:",
      "- https://github.com/jakobrosenberg/svelte-filerouter-example/blob/master/package.json",
      "- https://github.com/sveltejs/template/blob/master/package.json",
      "",
      "We prefer 'start-server-and-test' over 'npm-run-all' approach (just more explicit, than 'run-p' race).",
      "",
      "Also see:",
      "- https://github.com/svgdotjs/svg.js/blob/master/package.json"
    ],
    "scripts": {
      "": "Targets which are aimed to be implementation-detail (can change any time) have underscores (e.g. 'demo:_start')",
      "start": "Keep. Called by Rollup.'",
      "dev,build": "Temporary aliases until we get lib & demo detached."
    },
    "dependencies": {
      "sirv-cli, svelte-filerouter": "tbd. can these be in the 'devDependencies' side? https://github.com/akauppi/svg.rx.js/issues/10"
    },
    "devDependencies": {
      "concurrently": "Sweet!🍭 -> https://www.npmjs.com/package/concurrently"
    }
  }
}
